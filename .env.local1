# URL del Backend API
VITE_API_URL=http://localhost:5000/api

# URL del Backend para Socket.IO (sin /api)
VITE_SOCKET_URL=http://localhost:5000

# NOTA: En producción, configura estas variables de entorno con las URLs correctas del backend.
# Si VITE_SOCKET_URL no está definido, se derivará de VITE_API_URL removiendo '/api'.
# Si ninguna está definida, se usará un fallback que puede no funcionar en producción.

# Configuración adicional (opcional)
# VITE_APP_TITLE=Sistema de Tickets
# VITE_APP_VERSION=1.0.0